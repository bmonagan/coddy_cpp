#include <iostream>

int main() {
    // Declare your variables here
    // Tip: float uses the 'f' suffix (e.g. 4.5f), but double uses NO suffix (e.g. 3.1415)
    float radius = 4.5f;
    double pi = 3.1415;
    
    // Don't change below this line
    std::cout << "radius: " << radius << std::endl;
    std::cout << "pi: " << pi << std::endl;
    
    return 0;
}